### Neovim Personal Setup

1. Install neovim
   ```sh
   brew install neovim
   ```
2. Install this configuration
    ```sh
    git clone git@github.com:flavio-portela/vim.git ~/.config/nvim
    ```
3. Install [lazy vim](https://github.com/folke/lazy.nvim)

